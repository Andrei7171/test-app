{% extends 'base.html.twig' %}

{% block title %}Hello {{ name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>press {{ name }}! ✅</h1>

    {% if login %}
      <a href="{{ path("a2", { name: name }) }}" class="btn btn-lg btn-secondary fw-bold border-white bg-white">off {{name}}</a>
    {% else %}
      <a href="{{ routePath }}" class="btn btn-lg btn-warning fw-bold border-white bg-white">on {{name}}</a>
    {% endif %}
</div>
{% endblock %}
